{
  "name": "serviceconnect",
  "version": "0.1.0",
  "private": true,
  "description": "ServiceConnect – Plataforma de Profesionales & Oficios (SAP CAP)",
  "scripts": {
    "start": "cds run",
    "watch": "cds watch",
    "deploy": "cds deploy --to sqlite:db/sqlite.db"
  },
  "dependencies": {
    "@sap/cds": "^7.9.5",
    "express": "^4.18.2",
    "nodemailer": "^6.9.8",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "@sap/cds-dk": "^7.9.5",
    "@ui5/cli": "^4.0.38"
  },
  "engines": {
    "node": ">=18"
  },
  "cds": {
    "server": {
      "cors": {
        "origin": "*",
        "methods": "GET,HEAD,PUT,PATCH,POST,DELETE"
      }
    }
  }
}